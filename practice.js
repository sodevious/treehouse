
function practice() {
  
  var first = prompt("What's your first name?");
  var last = prompt("What's your last name?");
  
  var name = first + " " + last;

  alert("The string " + name.toUpperCase() + " is " + name.length + " number of chararacters long.")
};

practice();
